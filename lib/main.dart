import 'package:flutter/material.dart';
import 'package:proyect_325/src/Principal.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Principal();
  }
}